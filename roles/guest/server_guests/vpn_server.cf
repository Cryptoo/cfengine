body file control

{

  namespace => "cryptoo";

}

bundle agent vpn_server

{

  meta:

    "tags" slist => { "cryptoo_role" };

  methods:

    any::

      "configure system as a VPN server"
        usebundle => server_guest("/", "vpn_server", "x86_64", "no_multilib");

}