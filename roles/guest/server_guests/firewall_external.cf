body file control

{

  namespace => "cryptoo";

}

bundle agent firewall_external

{

  meta:

    "tags" slist => { "cryptoo_role" };

  methods:

    any::

      "configure system as a external firewall"
        usebundle => server_guest("/", "firewall_external", "x86_64", "no_multilib");

}
