body file control

{

  namespace => "cryptoo";

}

bundle agent gitlab(root)

{

  meta:

    "tags" slist => { "cryptoo_role" };

  methods:

    any::

      "configure system as an internal firewall"
        usebundle => server_guest("$(this.root)", "gitlab", "x86_64", "no_multilib");

}
