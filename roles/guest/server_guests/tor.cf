body file control

{

  namespace => "cryptoo";

}

bundle agent tor

{

  meta:

    "tags" slist => { "cryptoo_role" };

  methods:

    any::

      "configure system as a Tor node"
        usebundle => server_guest("/", "tor", "x86_64");

}
