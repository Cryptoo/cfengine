body file control

{

  namespace => "cryptoo";

}

bundle agent web

{

  meta:

    "tags" slist => { "cryptoo_role" };

  methods:

    any::

      "configure system as a web browser guest"
        usebundle => cryptoo:desktop_guest("/", "web", "x86_64", "nomultilib");

}
