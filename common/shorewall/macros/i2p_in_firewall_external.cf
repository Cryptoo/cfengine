body file control

{

  namespace => "cryptoo";

  inputs => {
              "cryptoo/config/config.cf",
              "cryptoo/config/file_names.cf",
              "cryptoo/config/directory_names.cf",
              "cryptoo/config/file_names.cf",
              "cryptoo/config/templates.cf",
              "cryptoo/util/classes.cf",
              "cryptoo/util/perms.cf"
            };

}

bundle agent shorewall_macro_i2p_in_firewall_external(root)

{

  meta:

    "tags" slist => { "shorewall_macro", "firewall_external" };

  vars:

    any::

      "macro_name"
        string => "i2p_in";

      "defined_numbers"
        data => readjson("$(sys.inputdir)/cryptoo/config/defined_numbers.json", 1M);

      "subnet_config"
        data => readjson("/$(file_names.subnet_config)", 1M);

    have_port_info::

      "port_info_string"
        string => readfile("/$(file_names.port_info_incoming)", 0);

      "port_info"
        data => parsejson("$(port_info_string)");

    i2p_port_info_evaluated::

      "macro_json"
        string => ifelse( "have_i2p_port_info",
          '{
             "name": "$(this.macro_name)",
             "default": "ACCEPT",
             "rules":
                  [
                   {
                     "action": "DNAT",
                     "source": "net",
                     "destination": "idmz:192.168.$(subnet_config[ispdmz]).$(defined_numbers[i2p]):$(port_info[i2p][internal])",
                     "protocol": "udp",
                     "destination_port": "$(port_info[i2p][external])"
                   },
                   {
                     "action": "DNAT",
                     "source": "net",
                     "destination": "idmz:192.168.$(subnet_config[ispdmz]).$(defined_numbers[i2p]):$(port_info[i2p][internal])",
                     "protocol": "tcp",
                     "destination_port": "$(port_info[i2p][external])"
                   }
                  ]
                }
           } ',
          '{
             "name": "$(this.macro_name)",
             "default": "ACCEPT"
           }'),
        classes => default:if_ok("have_macro_json");

  classes:

    have_port_info::

      "have_i2p_port_info"
        expression => regextract('"i2p":', "$(port_info_string)", "regextract_i2p");

      "i2p_port_info_evaluated"
        expression => "any";

    any::

      "have_port_info"
        expression => fileexists("/$(file_names.port_info_incoming)");

      "port_info_evaluated"
        expression => "any";

    port_info_evaluated&!have_port_info::

      "i2p_port_info_evaluated"
        expression => "any";

  files:

    have_macro_json::

      "$(root)$(directory_names.fs_config_shorewall)/macro.$(this.macro_name)"
        perms => system,
        create => "true",
        edit_template => "/$(templates.template[shorewall_macro])",
        template_method => "mustache",
        template_data => parsejson("$(macro_json)"),
        classes => shorewall;

}
