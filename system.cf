bundle agent system

{

  methods:

    role_master::

      "create top-level directory structure"
        usebundle => directories("/","master");

      "create a skeleton guest directory tree for building packages"
        usebundle => directories("/var/images/server/","guest");

}
